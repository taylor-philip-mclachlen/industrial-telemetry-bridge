new project, telemetry bridge. concept is agnostic observibility. a beautiful step from the bottleneck and ssytems behavior of the reverse proxy. goal is to use main C with some go routines. to start no hardwar just socat. 
going to take my time to reallly try to nail the memory allocation of C as this is the biggest factor when it come to these types of projects i want to make in the futre. go is just a handy side kick.

the potential challalges: 
IPC for  GO and C (inter-process communication) 
tty latency (termois) 
shared allignment (linux padding) 


first draft. raw byte stream: 

Sync Byte 1, offset 0 | 0x02 (STX) 
Motor RPM 4, offset 1 | unit32_t
Voltage 2, offset 5 | unit16_t 
Tempurature 2, offset 7 | init16_t 
CRC-8 1, offset 9 | unit8_t



to stop the OS from eating the synced bytes i have to run termois in raw mode: 
VMIN = 1 VTIME =0 

the C core will be higly deterministic, so the parced binary structure over the unix domain socket keeps latancy low. 

the go supervisor will need to read the stream and amp it binary.read() then goroutine pipeline for telemetry

socat config will be important to mock our industrial application. 
 
we will decide opentelesymtry later, as its agnostic behavior is a stand out freature. 

SCADA will be obvious obtion for ingress. we will talk later about this.
Main.go - Modbus TCP - SCADA 





     Socat -  Multipurpose relay (SOcket CAT)
     is  a  command  line based utility that establishes two bidirectional byte streams and transfers
     data between them. Because the streams can be constructed from a large set of different types  of  data
     sinks  and  sources  (see  address  types),  and  because lots of address options may be applied to the
     streams, socat can be used for many different purposes.
 


lets break this all down for learning purposes. 

1. The 1-Byte Sync & The "Padding Byte" Nightmare
Yes, this is exactly where padding bytes will completely corrupt your data if you aren't careful.

When a CPU reads data from RAM, it prefers to read it in chunks that match its architecture size (usually 4-byte or 8-byte boundaries on 32-bit and 64-bit Linux systems). This is called memory alignment.

By default, if you declare a struct like this:
struct Telemetry {
    uint8_t  sync_byte;   // 1 byte
    uint32_t motor_rpm;   // 4 bytes
};

The compiler looks at motor_rpm and says, "I want this 4-byte integer to start on a memory address that is a multiple of 4." Because sync_byte only took up 1 byte, the compiler will secretly inject 3 invisible padding bytes between sync_byte and motor_rpm to align it.

What you expect to send over the wire: [0x02][RPM][RPM][RPM][RPM] (5 bytes)

What C expects in memory by default: [0x02][PAD][PAD][PAD][RPM][RPM][RPM][RPM] (8 bytes)

If your mock machinery sends 5 bytes, but your C core expects 8 bytes because of padding, your data offsets get completely misaligned, and your variables will read garbage. Using #pragma pack(1) forces the compiler to strip those invisible padding bytes, ensuring 1 byte on the wire equals exactly 1 byte in memory.


2. Motor RPM (4 Bytes) & Motor ControlYou hit the nail on the head. In an industrial gateway, the Motor RPM (Rotations Per Minute) is your primary dynamic metric tied directly to the physical motor controller (like a VFD—Variable Frequency Drive).We allocate 4 bytes (32 bits) for this because industrial motors, turbines, or high-speed spindles can spin at tens of thousands of RPM, and we often need to transmit this value with high precision (sometimes scaled to include fractions of an RPM, like RPM * 100 to get two decimal places). A 32-bit integer gives us plenty of headroom (up to $4,294,967,295$), ensuring the telemetry never truncates or overflows, no matter how fast the machinery spins.

3. Voltage (Offset of 5) & Motor Potential
The "Offset of 5" simply means that if you look at a packet of raw bytes arriving in a sequence, the Voltage data starts exactly at the 6th byte (index 5, since we start counting at 0).

In industrial telemetry, Voltage represents the electrical potential being delivered to or generated by the system. Monitoring this is critical for two reasons:

Power Quality: Detecting voltage sags or surges that could damage the hardware.

Load Correlation: By pairing Voltage with RPM, your Go supervisor can calculate if the motor is drawing too much power for the speed it's running at, which usually indicates mechanical stress (like a jammed belt or a failing bearing).

4. Temperature & The Zone of OperationThe Temperature metric (2 bytes / 16-bit signed integer) defines your physical envelope—your safe Zone of Operation.Industrial equipment has strict thermal limits. If a motor or ambient enclosure gets too hot, insulation melts and electronics fail. We use a signed integer (int16_t) here because industrial environments aren't always hot; gateways are deployed in sub-zero outdoor environments (like wind turbines in winter). A signed 16-bit integer perfectly captures a range from $-32,768$ to $+32,767$, allowing you to read negative temperatures down to the exact degree or milli-degree.

5. What is CRC-8? (The Security Guard)
CRC-8 stands for Cyclic Redundancy Check (8-bit). Think of it as a mathematically rigorous security guard checking ID badges at the end of the packet.

When you stream raw bytes over long industrial serial cables (or even a virtual socket), electrical noise, interference, or timing glitches can flip a bit (turning a 0 into a 1). If a bit flips in your Voltage or RPM data, your gateway might suddenly think the machine is exploding when it's actually fine.

How it works:

Before sending the packet, the mock machinery runs all the data bytes (Sync + RPM + Volts + Temp) through a mathematical formula (a polynomial division) which spits out a single 1-byte remainder. This is the CRC-8 byte, tacked onto the very end of the packet.

When your C core receives the packet, it runs the exact same math formula over the data bytes.

The C core compares its result to the CRC-8 byte sent at the end of the packet.

If they match, the packet is perfectly intact. If a single bit was corrupted during transmission, the math fails, the C core drops the packet immediately, and it never pollutes your Go supervisor with bad data.



ok we have read, and thought lets start with the lowest layer and work our way up. its 27/06/2026 let see how long this project takes, first repo took me quite a few months due to inactivity and losing intrests i expect the same, reguardless i think the gain of understanding and building my portfoilio will be a accomplishemnt in the end.


protocol.h, lets talk about its funtions line by line: 

#ifndef ITB_PROTOCOL_H 
#define ITB_PROTOCOL_H 
...
#endif 

Include Guard: these line prevent the complier from duplicating the code, which would cause and "identity crisis. if the go project includes this file multiple times across differnt modules. 

#include <stdint.h>
#include <stddef.h>

Standard C Integers: (int, long) changes size depedant on the system, 32-bit / 64-bit. stdint.h gives explicit, fixed-width types like uint8_t (exactly 8bits/1byte) and uint32_t(exactly 32bits/4bytes) this guarentees the code runs exactly on the laptop or controller. 

#define FRAME_SYNC_BYTE 0X002 
Define Constant Macro: everywhere the compilier sees FRAME_SYNC_BYTE, it leaves behind a raw 0x02 byte. this is the "start of the text" flag the parser looks for to know if the new machine packet is staring. 

#pragma pack(push, 1) 
Overriding the memory controller: tells the compilier to temporaily change its rules. it pushes the current alignment state onto an internal compiler stack and enforces a strick 1-byte boundary alignment, this strips out all hidden layout padding. 

typedef struct {
    uint8_t sync_byte;     // 1 byte
    uint32_t motor_rpm;    // 4 bytes
    uint16_t voltage;      // 2 bytes
    int16_t tempurature;   // 2 bytes
    uint8_t crc8;          // 1 byte
} TelemetryPacket;

Data Blueprint Struct: maps the variables sequentially in memory. because of #pragma pack, motor_rpm start exactly 1 byte after sync_byte. voltage starts exactly 3 bytes after that. this guarantees tha tthe struct matches the raw 100byte binary stream exactly. 

#pragma pack(pop)
Restore compilier:normal compilier memory optimization aligmnet rules for the rest of any code that follows this block

static inline uint8_t compute_crc8(cont uint8_t *data, size_t length) 
inline function optimization: inline bypasses a standard function call overhead. insted of making the cpu jump across its memory space to exicute the code, it injects the logic directly inside your loops for lighting-fast parsing. const uint8_t *data provides read-only acess to the raw bytes. 

crc ^= data[i];
CRC bit shifting engine: feeds the nect byte of the stream into your CRC accumulator using an XOR gate op

for (int bit = 0; bit < 8; bit++) {
    if (crc & 0x80) {
        crc = (crc << 1) ^ 0x31; 
    } else { 
        crc <<= 1; 
    }
}
Shift the accumilator: 1 bit to the left(<<1) if the leftmost bit was a 1 (crc & 0x80), it applies an zor binary division againt your industrial polynomial (0x31). this scrambles the bits mathamaticlly, creating a highly sensitive mathematical seal over your data. 


 
